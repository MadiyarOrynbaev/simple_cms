<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to image_tag("http://placehold.it/200x50"), root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav pull-right">
        <li><%= link_to "Главная",   root_path %></li>
        <li><%= link_to "О Нас", about_path %></li>
        
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Разделы сайта <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% @subjects.each do |s| %>
              <li><%= link_to s.title, s %></li>
            <% end %>
          </ul>
        </li>

        <% if logged_in? %>
          <li><%= link_to "Админ", admin_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>